#!/usr/bin/env python3

print("\tThe Program Starts \n")

for i in range(2,10,2):#{ The first number is what it will start with the third number is what it will increment by
    # i+=1 we can use this to inciment up from 1 to 10. We don't really need it if using the below string but the below will start at 0
 print("\tWe are at " + str(i))
 print(i)   #if you remove the indent for print it will only print the last value
 #}
print("\tThe End of the Program")

#!/usr/bin/env python3

for i in range (25):#{
    if (i == 20):#{  This will skip twenty using the continue operator.
    #if (i % 2 == 0): #This will print odd numbers up to 25 using a modulo.
     #break #This breaks out of the loop at whater you make i
     continue #This contines if you use a nmber like above. 
    #}
    print("We are at number:" + str(i))
    #}
